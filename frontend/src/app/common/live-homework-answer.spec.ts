import { LiveHomeworkAnswer } from './live-homework-answer';

describe('LiveHomeworkAnswer', () => {
  it('should create an instance', () => {
    expect(new LiveHomeworkAnswer()).toBeTruthy();
  });
});
