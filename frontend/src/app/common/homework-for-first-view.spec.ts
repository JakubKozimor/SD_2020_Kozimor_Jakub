import { HomeworkForFirstView } from './homework-for-first-view';

describe('HomeworkForFirstView', () => {
  it('should create an instance', () => {
    expect(new HomeworkForFirstView()).toBeTruthy();
  });
});
