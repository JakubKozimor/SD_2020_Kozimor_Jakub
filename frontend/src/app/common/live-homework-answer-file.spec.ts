import { LiveHomeworkAnswerFile } from './live-homework-answer-file';

describe('LiveHomeworkAnswerFile', () => {
  it('should create an instance', () => {
    expect(new LiveHomeworkAnswerFile()).toBeTruthy();
  });
});
