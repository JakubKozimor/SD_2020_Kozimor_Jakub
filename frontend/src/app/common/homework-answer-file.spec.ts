import { HomeworkAnswerFile } from './homework-answer-file';

describe('HomeworkAnswerFile', () => {
  it('should create an instance', () => {
    expect(new HomeworkAnswerFile()).toBeTruthy();
  });
});
