import { GroupMessageFromSearch } from './group-message-from-search';

describe('GroupMessageFromSearch', () => {
  it('should create an instance', () => {
    expect(new GroupMessageFromSearch()).toBeTruthy();
  });
});
